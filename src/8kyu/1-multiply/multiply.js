/**
 * Return the multiplication between 2 number
 * 
 * @param {number} a 
 * @param {number} b 
 * 
 * @return {number}
 */
const multiply = (a, b) => {
    return a * b
};


module.exports = multiply;