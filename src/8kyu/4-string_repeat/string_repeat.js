/**
 * Return a string that exactly repeats the given src string
 * 
 * @param {number} number 
 * @param {string} string 
 */
const repeatStr = (number, string) => {
    return string.repeat(number);
};


module.exports = repeatStr;